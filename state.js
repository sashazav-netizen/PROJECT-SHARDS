/**
 * =========================================================
 * PROJECT SHARDS
 * Game State
 *
 * גרסה: 0.4.0
 *
 * אחראי על:
 * - נתוני השחקן
 * - פרופיל שחקן
 * - משאבים
 * - התקדמות
 * - מצב המשחק (ה"מה")
 *
 * אין כאן UI.
 * אין כאן HTML.
 * אין כאן מערכת קרב.
 *
 * חשוב — גבול אחריות (עודכן בגרסה 0.4.0):
 * שמירה/טעינה בפועל (ה"איך") עברו במלואן ל-save-system.js.
 * state.js לא נוגע יותר ב-localStorage ישירות.
 * הוא רק מכין snapshot לשמירה, ומטמיע בחזרה נתונים שנטענו.
 * כך אפשר בעתיד להחליף את שכבת האחסון (Cloud Save וכו')
 * בלי לגעת בקובץ הזה בכלל.
 * =========================================================
 */


import {

    שמורמשחק
        as
    שמורלאחסון,

    טעןמשחק
        as
    טעןמאחסון,

    יששמירה,

    מחקשמירה,

    קבלמידעשמירה
        as
    קבלמידעשמירהמאחסון

} from "./save-system.js";


/* =========================================================
   הגדרות כלליות
========================================================= */

const גרסת_מצב =
    "0.4.0";


/* =========================================================
   יצירת מצב ברירת מחדל
========================================================= */

/**
 * הפונקציה מחזירה מצב משחק חדש לחלוטין.
 *
 * חשוב:
 * אנחנו משתמשים בפונקציה ולא באובייקט אחד קבוע,
 * כדי שנוכל בעתיד לאפס משחק בצורה בטוחה.
 */

function צורמצברירתמחדל() {

    return {

        גרסה:
            גרסת_מצב,


        /* ---------------------------------------------
           פרופיל שחקן
        --------------------------------------------- */

        פרופיל: {

            קיים: false,

            שם: "",

            תאריך_יצירה: null,

            תאריך_שמירה_אחרונה: null

        },


        /* ---------------------------------------------
           נתוני שחקן
        --------------------------------------------- */

        שחקן: {

            שם: "נוסע",

            רמה: 1,

            ניסיון: 0,

            ניסיון_לרמה_הבאה: 100,

            כסף: 0,

            זהב: 0,

            יהלומים: 0

        },


        /* ---------------------------------------------
           התקדמות
        --------------------------------------------- */

        התקדמות: {

            אזור_נוכחי: 0,

            שלב_נוכחי: 0,

            קרבות_שהושלמו: 0,

            בוס_הובס: 0,

            מסעות_שהושלמו: 0,

            מפה_אינדקס: 0,

            מפה_נפילה_עד: null,

            מפה_נופל_גיבור_id: null

        },


        /* ---------------------------------------------
           מערכות תוכן
        --------------------------------------------- */

        גיבורים: [],

        קבוצת_גיבורים: [],

        קלפים: [],

        חפצים: [],

        סקילים: [],

        חפיסה: [],

        הישגים: [],

        משימות: [],


        /* ---------------------------------------------
           הגדרות
        --------------------------------------------- */

        הגדרות: {

            שפה: "he",

            צלילים: true,

            מוזיקה: true

        },


        /* ---------------------------------------------
           מצב זמני
        --------------------------------------------- */

        מצב: {

            מסך: "טעינה",

            בתוך_קרב: false,

            קרב_נוכחי: null

        }

    };

}


/* =========================================================
   מצב המשחק
========================================================= */

const מצבמשחק =
    צורמצברירתמחדל();


/* =========================================================
   קבלת מצב המשחק
========================================================= */

function קבלמצב() {

    return מצבמשחק;

}


/* =========================================================
   עדכון נתוני השחקן
========================================================= */

function עדכןשחקן(
    נתונים = {}
) {

    if (
        !נתונים ||
        typeof נתונים !== "object" ||
        Array.isArray(נתונים)
    ) {

        return false;

    }


    Object.assign(

        מצבמשחק.שחקן,

        נתונים

    );


    return true;

}


/* =========================================================
   שינוי משאב
========================================================= */

function שנהמשאב(
    סוג,
    כמות
) {

    if (
        !(סוג in מצבמשחק.שחקן)
    ) {

        console.warn(
            `[State] משאב לא קיים: ${סוג}`
        );


        return false;

    }


    if (
        !Number.isFinite(כמות)
    ) {

        console.warn(
            `[State] כמות משאב לא חוקית: ${כמות}`
        );


        return false;

    }


    const ערךנוכחי =
        Number(
            מצבמשחק.שחקן[סוג]
        );


    const ערךחדש =
        ערךנוכחי + כמות;


    מצבמשחק.שחקן[סוג] =
        Math.max(
            0,
            ערךחדש
        );


    return true;

}


/* =========================================================
   שינוי מצב מסך
========================================================= */

function שנהמסך(
    מסךחדש
) {

    if (
        typeof מסךחדש !== "string"
    ) {

        return false;

    }


    מצבמשחק.מצב.מסך =
        מסךחדש;


    return true;

}


/* =========================================================
   יצירת פרופיל חדש
========================================================= */

function צורפרופיל(
    שם
) {

    if (
        typeof שם !== "string"
    ) {

        return false;

    }


    const שםנקי =
        שם.trim();


    /* ---------------------------------------------
       בדיקת אורך
    --------------------------------------------- */

    if (
        שםנקי.length < 2
    ) {

        console.warn(
            "[Profile] השם חייב להכיל לפחות 2 תווים."
        );


        return false;

    }


    if (
        שםנקי.length > 20
    ) {

        console.warn(
            "[Profile] השם יכול להכיל עד 20 תווים."
        );


        return false;

    }


    const עכשיו =
        new Date().toISOString();


    /* ---------------------------------------------
       יצירת פרופיל
    --------------------------------------------- */

    מצבמשחק.פרופיל = {

        קיים: true,

        שם:
            שםנקי,

        תאריך_יצירה:
            עכשיו,

        תאריך_שמירה_אחרונה:
            עכשיו

    };


    /* ---------------------------------------------
       סנכרון עם נתוני השחקן
    --------------------------------------------- */

    מצבמשחק.שחקן.שם =
        שםנקי;


    return true;

}


/* =========================================================
   בדיקת פרופיל
========================================================= */

function ישפרופיל() {

    return (

        מצבמשחק.פרופיל &&

        מצבמשחק.פרופיל.קיים === true &&

        typeof מצבמשחק.פרופיל.שם === "string" &&

        מצבמשחק.פרופיל.שם.trim().length >= 2

    );

}


/* =========================================================
   קבלת שם השחקן
========================================================= */

function קבלשםשחקן() {

    if (
        ישפרופיל()
    ) {

        return מצבמשחק.פרופיל.שם;

    }


    return מצבמשחק.שחקן.שם;

}


/* =========================================================
   האם קיים משחק שמור
   (מאציל בדיקה בפועל ל-save-system.js)
========================================================= */

function ישמשחקקיים() {

    return יששמירה();

}


/* =========================================================
   שמירת המשחק
   (state.js מכין snapshot, save-system.js שומר אותו)
========================================================= */

function שמורמשחק() {

    const עכשיו =
        new Date().toISOString();


    /* ---------------------------------------------
       עדכון זמן שמירה — רק אם יש פרופיל פעיל
    --------------------------------------------- */

    if (
        מצבמשחק.פרופיל &&
        מצבמשחק.פרופיל.קיים === true
    ) {

        מצבמשחק.פרופיל.תאריך_שמירה_אחרונה =
            עכשיו;

    }


    return שמורלאחסון(
        מצבמשחק
    );

}


/* =========================================================
   טעינת המשחק
   (save-system.js מחזיר נתונים גולמיים,
   state.js מטמיע אותם למצב החי)
========================================================= */

function טעןמשחק() {

    const נתונים =
        טעןמאחסון();


    if (
        !נתונים
    ) {

        return false;

    }


    /* ---------------------------------------------
       פרופיל
    --------------------------------------------- */

    if (
        נתונים.פרופיל &&
        typeof נתונים.פרופיל === "object"
    ) {

        Object.assign(

            מצבמשחק.פרופיל,

            נתונים.פרופיל

        );

    }


    /* ---------------------------------------------
       שחקן
    --------------------------------------------- */

    if (
        נתונים.שחקן &&
        typeof נתונים.שחקן === "object"
    ) {

        Object.assign(

            מצבמשחק.שחקן,

            נתונים.שחקן

        );

    }


    /* ---------------------------------------------
       סנכרון שם
    --------------------------------------------- */

    if (
        ישפרופיל()
    ) {

        מצבמשחק.שחקן.שם =
            מצבמשחק.פרופיל.שם;

    }


    /* ---------------------------------------------
       התקדמות
    --------------------------------------------- */

    if (
        נתונים.התקדמות &&
        typeof נתונים.התקדמות === "object"
    ) {

        Object.assign(

            מצבמשחק.התקדמות,

            נתונים.התקדמות

        );

    }


    /* ---------------------------------------------
       מערכות תוכן
    --------------------------------------------- */

    const מערכות = [

        "גיבורים",

        "קבוצת_גיבורים",

        "קלפים",

        "חפצים",

        "סקילים",

        "חפיסה",

        "הישגים",

        "משימות"

    ];


    for (
        const מערכת
        of מערכות
    ) {

        if (
            Array.isArray(
                נתונים[מערכת]
            )
        ) {

            מצבמשחק[מערכת] =

                structuredClone(
                    נתונים[מערכת]
                );

        }

    }


    /* ---------------------------------------------
       הגדרות
    --------------------------------------------- */

    if (
        נתונים.הגדרות &&
        typeof נתונים.הגדרות === "object"
    ) {

        Object.assign(

            מצבמשחק.הגדרות,

            נתונים.הגדרות

        );

    }


    /* ---------------------------------------------
       מצב זמני
       לא טוענים קרב שהיה באמצע.
    --------------------------------------------- */

    מצבמשחק.מצב.בתוך_קרב =
        false;


    מצבמשחק.מצב.קרב_נוכחי =
        null;


    return true;

}


/* =========================================================
   מחיקת משחק שמור
========================================================= */

function מחקמשחקשמור() {

    return מחקשמירה();

}


/* =========================================================
   איפוס מצב המשחק
========================================================= */

function אפסמצבמשחק() {

    const מצבחדש =
        צורמצברירתמחדל();


    /* ---------------------------------------------
       החלפת כל הנתונים
    --------------------------------------------- */

    Object.keys(
        מצבמשחק
    ).forEach(
        מפתח => {

            מצבמשחק[מפתח] =
                מצבחדש[מפתח];

        }
    );


    return true;

}


/* =========================================================
   קבלת מידע על השמירה
========================================================= */

function קבלמידעשמירה() {

    return קבלמידעשמירהמאחסון();

}


/* =========================================================
   Export
========================================================= */

export {

    קבלמצב,

    עדכןשחקן,

    שנהמשאב,

    שנהמסך,

    צורפרופיל,

    ישפרופיל,

    קבלשםשחקן,

    ישמשחקקיים,

    שמורמשחק,

    טעןמשחק,

    מחקמשחקשמור,

    אפסמצבמשחק,

    קבלמידעשמירה

};
